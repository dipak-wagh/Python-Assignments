# Explain why the following code works without declaration:

x = 100
# compare this with C or Java 

# above code is works without declaration, because python is dynamically typed language, 
# it analyze typeof data at runtime.
