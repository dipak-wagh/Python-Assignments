# Explain how python manages memory automatically. 
# Why does the programmar not need to explicitly allocate or free memory

# Python manages memory automatically using dynamic memory allocation and 
# garbage collection, so the programmer does not need to explicitly allocate or free memory.